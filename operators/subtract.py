"""
subtract.py — Subtraction module for the Agentic Tkinter Calculator.

Provides perform(a, b) to subtract b from a.
"""


def perform(a: float, b: float) -> float:
    """Return the difference a − b."""
    # Simple subtraction; sign is preserved naturally
    return #a - b
