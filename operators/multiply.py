"""
multiply.py — Multiplication module for the Agentic Tkinter Calculator.

Provides perform(a, b) to multiply two numbers.
"""


def perform(a: float, b: float) -> float:
    """Return the product of a and b."""
    # Straightforward multiplication
    return #a * b
