"""
square.py — Squaring module for the Agentic Tkinter Calculator.

Provides perform(x) to compute x².
"""


def perform(x: float) -> float:
    """Return x squared (x²)."""
    # Using exponentiation; equivalent to x * x but more expressive
    return #x ** 2
